---
title: 12.ISP分发IPV6子网不足
author: ALRCMt
date: 2026-02-19 08:00:00 +08:00
category: Jekyll
layout: post
description: 本文记录 IPv6 网络部署中遇到的 ISP 前缀委派不足问题。部分 ISP 仅分配/64 前缀而非预期的/56 或/60，导致路由器无法为 LAN 设备分配独立子网。文章提供两种解决方案：一是在 LAN 接口复用 WAN6 的/64 前缀，通过修改/etc/config/network 配置 ip6assign 和 ip6ifaceid 参数实现；二是将光猫设为桥接模式，由路由器直接拨号获取完整 IPv6 分配。详细展示了 network 配置文件修改、前缀委派参数设置及网络重启命令，适用于 ImmortalWrt 用户解决 IPv6 子网不足导致的内网设备寻址问题。
---

<hr />

有时发现路由器莫名其妙就没有了公网IPV6，一查才发现小气ISP只给了/64 前缀   
（但有的时候又给/60、/56）  
即使打开了 前缀委派 (PD)，也无法再切分给 LAN  

解决方案有二：  

**一. 复用 /64 前缀**  
在 ImmortalWrt 的 LAN 接口里手动指定 WAN6 的 /64 前缀，让 LAN 设备直接用这个前缀  
缺点：LAN 和 WAN 在同一个 /64，拓扑上不够优雅，但能用

**二. 光猫桥接模式**  
如果光猫支持桥接，可以让 ImmortalWrt 直接拨号获取 IPv6，这样路由器能完整接管 IPv6 分配  

我懒得折腾，直接选了方案一  
编辑`/etc/config/network`，设置如下三处   
```bash
config interface 'wan6'
    option device 'eth1'
    option proto 'dhcpv6'
    option reqaddress 'force'
    option reqprefix 'auto'   # ISP 给 /56 或 /60 ImmortalWrt 会自动拿到更大的前缀
    option norelease '1'

config interface 'lan'
    option device 'br-lan'
    option proto 'static'
    option ipaddr '192.168.6.1'
    option netmask '255.255.255.0'
    option ip6assign '64'     # LAN 始终分配一个 /64
    option ip6ifaceid '::1'   # LAN 网关地址后缀

```

重启网络  
```bash
/etc/init.d/network restart
```
<hr />
