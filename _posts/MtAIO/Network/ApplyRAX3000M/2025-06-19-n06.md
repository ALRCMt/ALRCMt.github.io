---
title: 6.PVE电源控制面板
author: ALRCMt
date: 2025-06-19 08:00:00 +08:00
category: Jekyll
layout: post
description: RAX3000M 软路由创建 PVE 电源控制面板教程，利用 uhttpd 实现远程控制宿主机电源管理的网页服务。文章详细说明了 uhttpd Web 服务器配置、CGI 脚本编写方法、SSH 免密登录设置、关机命令安全执行机制以及网页界面美化步骤，并提供访问权限控制、HTTPS 加密配置、操作日志记录、防误触保护以及多用户管理功能，帮助用户通过路由器 Web 界面便捷地管理 PVE 服务器电源状态。
---

<hr />


利用ImmortalWrt自带的 uhttpd 来托管网页和执行脚本

``` shell
# 1. 创建网页根目录
mkdir -p /mnt/pve_web
# 2. 创建CGI脚本目录（将用于存放可执行脚本链接）
mkdir -p /mnt/pve_web/cgi-bin

# 检查uhttpd是否在运行（通常80端口已被LuCI占用）
netstat -tulpn | grep uhttpd
# 如果有多个，先停止所有
killall uhttpd
# 启动新的
uhttpd -p 18080 -h /mnt/pve_web -c /mnt/pve_web/cgi-bin &

# 验证
netstat -tlnp | grep 18080
```
然后创建HDMI文件与CGI脚本,我的目录结构如下  
不要忘记赋予操作权限  

```
 /mnt/pve_web/  
      ├── index.html  
      ├── favicon.ico  
      └── cgi-bin/  
         ├── ping_server.sh  
         ├── pve_shutdown.sh  
         └── pve_wake.sh  
```

具体脚本内容懒得放了，可以直接照抄我的，里面的IP、MAC及用户名密钥之类别忘记改了   
WEB文件：[https://github.com/ALRCMt/MtAIO-Build/tree/main/pve_web](https://github.com/ALRCMt/MtAIO-Build/tree/main/pve_web)   
要注意的点：脚本使用了 `shpass` `etherwake` 两个包，如果没有请安装  


效果图：

<figure class="image-preview">
  <a href="https://github.com/ALRCMt/MtAIO-Build/raw/main/images/2462.png" class="preview-link">
    <img src="https://github.com/ALRCMt/MtAIO-Build/raw/main/images/2462.png" alt="" width="600px">
  </a>
</figure>

以下为后进阶的版本，若没有切换系统的要求上面就够了  
**[控制面板PLUS：增加切换系统与检测系统模式功能](/jekyll/2026-02-19-d10.html)**  

<hr />
