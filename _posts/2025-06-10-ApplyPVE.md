---
title: PVE系统配置
author: ALRCMt
date: 2025-06-10
category: Jekyll
layout: post
---

<hr />

### 1.设置 PVE 的 APT 源

**8.x 版本设置**
PVE 的默认软件源是他的企业服务地址，我们个人使用需要将其换成国内的软件源  
可以在[https://help.mirrors.cernet.edu.cn/proxmox](https://help.mirrors.cernet.edu.cn/proxmox) 换源  
编辑`/etc/apt/sources.list`，替换如下

```shell
deb https://mirrors.ustc.edu.cn/debian bookworm main contrib
deb https://mirrors.ustc.edu.cn/debian bookworm-updates main contrib
deb https://mirrors.ustc.edu.cn/debian-security bookworm-security main contrib
```

将 PVE 的企业源 `/etc/apt/sources.list.d/pve-enterprise.sources` 注释掉  
将 PVE 的 Ceph 源`/etc/apt/sources.list.d/ceph.list`，替换如下

```shell
deb https://mirrors.tuna.tsinghua.edu.cn/proxmox/debian/ceph-quincy bookworm no-subscription
```

在`/etc/apt/sources.list.d/`下创建 pve-no-sub.list 文件，填上以下内容

```shell
deb https://mirrors.tuna.tsinghua.edu.cn/proxmox/debian bookworm pve-no-subscription
```

**9.x 版本设置**
在`/etc/apt/sources.list.d/debian.sources `中注释掉原有配置，添加以下

```shell
Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/debian
Suites: trixie trixie-updates trixie-backports
Components: main contrib non-free non-free-firmware
Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg

Types: deb-src
URIs: https://mirrors.tuna.tsinghua.edu.cn/debian
Suites: trixie trixie-updates trixie-backports
Components: main contrib non-free non-free-firmware
Signed-By: /usr/share/keyrings/debian-archive-keyring.gpg
```

> 因为 9.x 版本 BUG 太多了，这下面的源换不换随便，我也不清楚  
> 将 PVE 的企业源 `/etc/apt/sources.list.d/pve-enterprise.sources` 注释掉  
> 将 PVE 的 Ceph 源 `/etc/apt/sources.list.d/ceph.sources` 也替换成清华源
```shell
Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/proxmox/debian/ceph-squid
Suites: trixie
Components: main
Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
```
> 在 `/etc/apt/sources.list.d` 目录下创建 pve-no-subscription.sources 文件，填上以下内容
```shell
Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/proxmox/debian/pve
Suites: trixie
Components: pve-no-subscription
Signed-By: /usr/share/keyrings/proxmox-archive-keyring.gpg
```

> 如果复制粘贴修改 apt 源后报错 [**通常原因**](/jekyll/2025-06-30-LoveFormMe.html#06pve-%E6%9B%B4%E6%8D%A2-apt-%E6%BA%90%E5%90%8E%E6%8A%A5%E9%94%99)

<hr />

### 2.更换默认IP与自动获取IPV6

编辑`/etc/network/interfaces`  
修改IP地址：找到包含address的行，修改为新的IP地址  
如果需要更改网关，也要修改gateway这一行，完成后保存并退出   
重启网络 `systemctl restart networking`  
如果你有闲心，也可以把`/etc/hosts`和`/etc/issue`也改了，使开机显示IP与实际一致  
意义不大~~我有强迫症~~

如果你有使用IPV6的需要，那么需要配置IPV6  
我选择的是直接通过网桥接收路由器广播的RA（路由通告），使用SLAAC自动配置生成地址  
你需要首先修改路由器相关内容：[**SLAAC自动分配IPV6**](/jekyll/2025-06-14-ApplyRAX3000M.html#修改默认软件源)  
然后修改`/etc/network/interfaces`  
``` shell
auto vmbr0
iface vmbr0 inet static
        address 192.168.6.217/24
        gateway 192.168.6.1
        bridge-ports enp34s0
        bridge-stp off
        bridge-fd 0
iface vmbr0 inet6 auto #添加这一项
```
`systemctl restart networking` 重启网络  

再修改 `/etc/sysctl.conf`,末尾添加以下两行
``` shell
net.ipv4.ip_forward=1
net.ipv6.conf.vmbr0.accept_ra = 2
```
`sysctl -p` 应用并重启

请注意！现在可能虚拟机会断网，你需要reboot完全重启PVE，之后就会正常

`ip -6 addr show` 查看是否获得了公网IPV6地址，再外网ping一下试试

<hr />

### 3.开启网络唤醒 WOL

需要在 BIOS 中开启 WOL 功能，各主板设置方法不同，自己上网查去  
默认情况下，PVE 的网络唤醒是禁用的，需要手动打开才可以网络唤醒

安装 ethtool 工具

```shell
apt install ethtool
```

查看网卡信息

```shell

ethtool [网卡名称] # 观察输出结果Supports Wake-on 与 Wake-on

# supports wake-on 判断该网卡是否支持 WOL 唤醒，若值为 pumbg 则表示支持 WOL
# Wake-on 值为 d 则表示 WOL 禁用状态，g 则为开启，PVE 默认为 d
```

开启 WOL 网络唤醒

```shell
ethtool -s eth0 wol g
```

由于每次开机时，Wake-on 的值都会重置为 d，因此需要在开机时自动运行开启 WOL 的命令

编辑 /etc/rc.local 文件

```shell
#!/bin/bash
ethtool -s eth0 wol g
​
exit 0
```

赋予运行权限

```shell
chmod +x /etc/rc.local
```

 
<hr />

### 4.自动与远程开关机

原先我使用bios来自动开机，但是这样可操作性太低了  
目前推荐使用软路由来进行 [**自动开关机**]()  

> 这里保存是PVE系统内的自动关机教程，懒得删了，作为冗余吧  
> 编辑 root 用户的 crontab
``` shell
crontab -e
```
> 添加自动关机任务
``` shell
# 每天凌晨2:30执行关机
30 2 * * * /sbin/shutdown -h +5 "系统将在5分钟后关机进行日常维护，请保存您的工作"
```

至于远程开关机，也是利用了软路由创建了一个网页服务 [**PVE电源控制面板**](http://127.0.0.1:4000/jekyll/2025-06-14-ApplyRAX3000M.html#7pve%E7%94%B5%E6%BA%90%E9%9D%A2%E6%9D%BF%E7%BD%91%E9%A1%B5)

<hr />

### 5.硬盘更换、克隆

我原本的系统盘只有120G，后面换了一个英特尔480G的  
因为我新旧硬盘可以同时连接到PVE主机，所以我直接克隆硬盘  
**请注意，我的系统格式是LVM，如果你是ZFS，请不要照抄**  

``` shell
# 新硬盘安装到PVE主机后确认新硬盘的设备标识
fdisk -l
# 开始全盘克隆
dd if=/dev/旧盘标识 of=/dev/新盘标识 bs=1M status=progress
```

克隆完成后，关机，物理上取下旧硬盘，将新硬盘安装到旧硬盘的位置

``` shell
# 然后查看新盘的详细状态
fdisk -l /dev/硬盘标识

# 如果出现The backup GPT table is not on the end of the device. 
apt update 
apt install parted # 安装 partprobe 命令

gdisk /dev/sdb # 使用 gdisk 修复GPT分区表

Command: w  # 写入更改（这会修复GPT表）
Do you want to proceed? (Y/N): Y  # 确认

# 重新扫描磁盘分区
partprobe /dev/硬盘标识

# 验证修复
fdisk -l /dev/硬盘标识

parted /dev/硬盘标识 resizepart 3 100% # 调整分区大小

# 重新扫描分区表
partprobe /dev/sdb

# 扩展物理卷
pvresize /dev/sdb3

# 扩展逻辑卷组
vgextend pve /dev/sdb3

# 验证卷组现在有更多空间
vgs
```
<hr />

### 6.跨系统文件传输

有的时候，你可能需要跨系统进行文件传输  
比如我需要将某个硬盘的数据导入到TrueNAS上，但是数据太大不方便网络传输
所以可以将硬盘插到物理接口上，然后挂载这个硬盘

``` shell
# 创建挂载点目录
mkdir -p /mnt/external_drive

# 查看移动硬盘的分区信息
fdisk -l | grep '^Disk /dev\|^/dev/'

# 挂载移动硬盘（假设它是单个分区 /dev/sdc1）
mount -t auto /dev/sdc1 /mnt/external_drive  # 自动检测

mount -t ntfs /dev/sdc1 /mnt/external_drive  # NTFS 文件系统
mount -t vfat /dev/sdc1 /mnt/external_drive  # FAT32 文件系统
mount -t exfat /dev/sdc1 /mnt/external_drive # exFAT 文件系统

# 如果遇到 NTFS 文件系统的问题，可能需要安装 NTFS 支持
apt update
apt install ntfs-3g

# 确认挂载成功
df -h /mnt/external_drive
ls /mnt/external_drive
```
然后通过ssh传输到TrueNAS池里，如果反过来就把命令里两个文件夹对换
``` shell
# 使用 rsync（支持断点续传和进度显示）
rsync -av --progress /mnt/external_drive/ root@<TrueNAS_IP>:/path/to/destination/

#  如果未安装
apt update
apt install rsync

```
不知道为什么有可能会出现权限混乱的问题。我直接选择在TrueNAS的控制台把文件夹权限改了
``` shell
su root 
chmod -R 777 /mnt/池/文件夹
```
